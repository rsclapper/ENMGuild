@model IEnumerable<GuildENM.Models.Comment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            UserId
        </th>
        <th>
            Content
        </th>
        <th>
            DatePosted
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
			@item.UserId
        </td>
        <td>
			@item.Content
        </td>
        <td>
			@item.DatePosted
        </td>
    </tr>
}

</table>


